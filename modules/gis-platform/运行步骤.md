# 运行步骤

### 1.本地运行测试

**启动顺序**

1️⃣ 启动Kafka

新版 Kafka 分两种模式：

1. **KRaft 模式（不用 ZK）** → 必须加 `KAFKA_PROCESS_ROLES`
2. **ZooKeeper 模式（老版本）** → 需要先启动ZK

方案一：KRaft 模式

```powershell
docker run -d --name kafka \
-p 9092:9092 \
-p 9093:9093 \
-e KAFKA_BROKER_ID=1 \
-e KAFKA_HOST_NAME=localhost \
-e KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093 \
-e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092 \
-e KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT \
-e KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER \
-e KAFKA_PROCESS_ROLES=broker,controller \
-e KAFKA_CONTROLLER_QUORUM_VOTERS=1@localhost:9093 \
-e KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 \
-e ALLOW_PLAINTEXT_LISTENER=yes \
apache/kafka:latest
```

方案二：ZK模式

```powershell
# 命令行启动
docker run -d --name zookeeper \
-p 2181:2181 \
-e ZOOKEEPER_CLIENT_PORT=2181 \
-e ZOOKEEPER_TICK_TIME=2000 \
confluentinc/cp-zookeeper:7.4.0

docker run -d --name kafka \
-p 9092:9092 \
--link zookeeper \
-e KAFKA_BROKER_ID=1 \
-e KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181 \
-e KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092 \
-e KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1 \
confluentinc/cp-kafka:7.4.0
```

2️⃣ 启动 gis-worker（IDEA运行）
3️⃣ 启动 gis-api（IDEA运行）

测试命令：
curl -X POST http://localhost:8080/gis/upload -F "file=@D:\works\temp\data\test_shp.shp"
